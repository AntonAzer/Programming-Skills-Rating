# Leave blank/unset for both local dev (Vite proxy handles /api) and
# production on Vercel (frontend + backend share the same domain).
# Only set this if your backend is deployed on a DIFFERENT domain than
# the frontend, e.g. VITE_API_URL="https://your-backend.vercel.app"
VITE_API_URL=/api/analyze
